This directory contains protocol1 rat utils api


WHAT IT DOES?
=============
It provides protocol1 rat utils api.

HOW IT WAS BUILT?
==================

The libratconfig.so binary was built entirely from source files written by Mediatek.


HOW TO USE IT?
==============

Files in this directory is used to
generate a shared library 'libratconfig.so'.

The shared library 'libratconfig.so' is used by librilmtk.so

