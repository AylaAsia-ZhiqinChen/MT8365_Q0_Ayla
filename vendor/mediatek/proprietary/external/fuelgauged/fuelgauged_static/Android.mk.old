LOCAL_PATH:= $(call my-dir)

include $(CLEAR_VARS)

LOCAL_SRC_FILES:= \
	fgauge_main_2.cpp \
	fg_log.cpp

LOCAL_FORCE_STATIC_EXECUTABLE := true

ifeq ($(MTK_GAUGE_VERSION), 30)
LOCAL_STATIC_LIBRARIES := libfgauge_static_gm30 libcutils liblog libc
else
LOCAL_STATIC_LIBRARIES := libfgauge_static libcutils liblog libc
endif

LOCAL_MODULE:= fuelgauged_static
#bobule workaround pdk build error,  needing review
LOCAL_MULTILIB := 32
#LOCAL_MODULE_PATH := $(TARGET_ROOT_OUT)/sbin
include $(BUILD_EXECUTABLE)
