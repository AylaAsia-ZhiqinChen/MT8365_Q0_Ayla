This module creates the FM/MATV/VIB Audio Player libraries.

This library is pre-built by MediaTek internally.


WHAT IT DOES?
=============
FM/MATV/VIB Audio Player is used to render audio data.


HOW IT WAS BUILT?
==================
It needs the following library from AOSP:

libcutils, libutils, libmedia, libaudiosetting

All source/dependency modules of this module are already put in
'vendor/mediatek/libs' folder.

HOW TO USE IT?
==============

The file can not be be used directly by an application developer.

The majority of source code for this library were written by MediaTek.  Various
open source libraries are also used and contained in this folder.
