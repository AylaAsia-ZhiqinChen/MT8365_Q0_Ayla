// This source code is generated by UdpGeneratorTool, not recommend to modify it directly
package com.mediatek.location.lppe.network;

import java.util.Arrays;

import com.mediatek.socket.base.SocketUtils.BaseBuffer;
import com.mediatek.socket.base.SocketUtils.ProtocolHandler;
import com.mediatek.socket.base.SocketUtils.UdpServerInterface;
import com.mediatek.socket.base.UdpClient;

/**
 * This is an LPPe interface between Network and AGPSD <br>
 * agpsVersion <br>
 * 1 first MP version <br>
 * networkVersion <br>
 * 1 first MP version <br>
 */
public class LPPeNetwork {
    public final static int PROTOCOL_TYPE = 204;
    public final static int MAX_BUFF_SIZE = 46;
    public final static int REQUEST_CAPABILITIES = 0;
    public final static int PROVIDE_CAPABILITIES = 1;
    public final static int START_MEASUREMENT = 2;
    public final static int STOP_MEASUREMENT = 3;
    public final static int PROVIDE_MEASUREMENT = 4;

    public static class LPPeNetworkSender {
        public boolean requestCapabilities(UdpClient client, int agpsVersion) {
            synchronized(client) {
                if(!client.connect()) {
                    return false;
                }
                BaseBuffer buff = client.getBuff();
                buff.putInt(PROTOCOL_TYPE);
                buff.putInt(REQUEST_CAPABILITIES);
                buff.putInt(agpsVersion);
                boolean _ret = client.write();
                client.close();
                return _ret;
            }
        }

        public boolean provideCapabilities(UdpClient client, int networkVersion, boolean networkLocationSupported) {
            synchronized(client) {
                if(!client.connect()) {
                    return false;
                }
                BaseBuffer buff = client.getBuff();
                buff.putInt(PROTOCOL_TYPE);
                buff.putInt(PROVIDE_CAPABILITIES);
                buff.putInt(networkVersion);
                buff.putBool(networkLocationSupported);
                boolean _ret = client.write();
                client.close();
                return _ret;
            }
        }

        public boolean startMeasurement(UdpClient client) {
            synchronized(client) {
                if(!client.connect()) {
                    return false;
                }
                BaseBuffer buff = client.getBuff();
                buff.putInt(PROTOCOL_TYPE);
                buff.putInt(START_MEASUREMENT);
                boolean _ret = client.write();
                client.close();
                return _ret;
            }
        }

        public boolean stopMeasurement(UdpClient client) {
            synchronized(client) {
                if(!client.connect()) {
                    return false;
                }
                BaseBuffer buff = client.getBuff();
                buff.putInt(PROTOCOL_TYPE);
                buff.putInt(STOP_MEASUREMENT);
                boolean _ret = client.write();
                client.close();
                return _ret;
            }
        }

        public boolean provideMeasurement(UdpClient client, NetworkLocationMeasurement meas) {
            synchronized(client) {
                if(!client.connect()) {
                    return false;
                }
                BaseBuffer buff = client.getBuff();
                buff.putInt(PROTOCOL_TYPE);
                buff.putInt(PROVIDE_MEASUREMENT);
                buff.putCodable(meas);
                boolean _ret = client.write();
                client.close();
                return _ret;
            }
        }

    }

    public static abstract class LPPeNetworkReceiver implements ProtocolHandler {

        public abstract void requestCapabilities(int agpsVersion);

        public abstract void provideCapabilities(int networkVersion, boolean networkLocationSupported);

        public abstract void startMeasurement();

        public abstract void stopMeasurement();

        public abstract void provideMeasurement(NetworkLocationMeasurement meas);


        public boolean readAndDecode(UdpServerInterface server) {
            if (!server.read()) {
                return false;
            }
            return decode(server);
        }

        @Override
        public int getProtocolType() {
            return PROTOCOL_TYPE;
        }

        @Override
        public boolean decode(UdpServerInterface server) {
            boolean _ret = true;
            BaseBuffer buff = server.getBuff();
            buff.setOffset(4); // skip protocol type
            int _type = buff.getInt();
            switch (_type) {
            case REQUEST_CAPABILITIES: {
                int agpsVersion = (int) buff.getInt();
                requestCapabilities(agpsVersion);
                break;
            }
            case PROVIDE_CAPABILITIES: {
                int networkVersion = (int) buff.getInt();
                boolean networkLocationSupported = (boolean) buff.getBool();
                provideCapabilities(networkVersion, networkLocationSupported);
                break;
            }
            case START_MEASUREMENT: {
                startMeasurement();
                break;
            }
            case STOP_MEASUREMENT: {
                stopMeasurement();
                break;
            }
            case PROVIDE_MEASUREMENT: {
                NetworkLocationMeasurement meas = (NetworkLocationMeasurement) buff.getCodable(NetworkLocationMeasurement._instance);
                provideMeasurement(meas);
                break;
            }
            default: {
                _ret = false;
                break;
            }
            }
            return _ret;
        }
    }

}
