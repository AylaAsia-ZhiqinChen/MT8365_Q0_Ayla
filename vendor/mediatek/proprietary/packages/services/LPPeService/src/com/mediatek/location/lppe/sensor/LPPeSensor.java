// This source code is generated by UdpGeneratorTool, not recommend to modify it directly
package com.mediatek.location.lppe.sensor;

import java.util.Arrays;

import com.mediatek.socket.base.SocketUtils.BaseBuffer;
import com.mediatek.socket.base.SocketUtils.ProtocolHandler;
import com.mediatek.socket.base.SocketUtils.UdpServerInterface;
import com.mediatek.socket.base.UdpClient;

/**
 * This is an LPPe interface between Sensor and AGPSD <br>
 * agpsVersion <br>
 * 1 first MP version <br>
 * sensorVersion <br>
 * 1 first MP version <br>
 */
public class LPPeSensor {
    public final static int PROTOCOL_TYPE = 203;
    public final static int MAX_BUFF_SIZE = 23;
    public final static int REQUEST_CAPABILITIES = 0;
    public final static int PROVIDE_CAPABILITIES = 1;
    public final static int START_MEASUREMENT = 2;
    public final static int STOP_MEASUREMENT = 3;
    public final static int PROVIDE_PRESSURE_MEASUREMENT = 4;

    public static class LPPeSensorSender {
        public boolean requestCapabilities(UdpClient client, int agpsVersion) {
            synchronized(client) {
                if(!client.connect()) {
                    return false;
                }
                BaseBuffer buff = client.getBuff();
                buff.putInt(PROTOCOL_TYPE);
                buff.putInt(REQUEST_CAPABILITIES);
                buff.putInt(agpsVersion);
                boolean _ret = client.write();
                client.close();
                return _ret;
            }
        }

        public boolean provideCapabilities(UdpClient client, int sensorVersion, SensorProvideCapabilities capabilities) {
            synchronized(client) {
                if(!client.connect()) {
                    return false;
                }
                BaseBuffer buff = client.getBuff();
                buff.putInt(PROTOCOL_TYPE);
                buff.putInt(PROVIDE_CAPABILITIES);
                buff.putInt(sensorVersion);
                buff.putCodable(capabilities);
                boolean _ret = client.write();
                client.close();
                return _ret;
            }
        }

        public boolean startMeasurement(UdpClient client, SensorMeasurementRequest request) {
            synchronized(client) {
                if(!client.connect()) {
                    return false;
                }
                BaseBuffer buff = client.getBuff();
                buff.putInt(PROTOCOL_TYPE);
                buff.putInt(START_MEASUREMENT);
                buff.putCodable(request);
                boolean _ret = client.write();
                client.close();
                return _ret;
            }
        }

        public boolean stopMeasurement(UdpClient client) {
            synchronized(client) {
                if(!client.connect()) {
                    return false;
                }
                BaseBuffer buff = client.getBuff();
                buff.putInt(PROTOCOL_TYPE);
                buff.putInt(STOP_MEASUREMENT);
                boolean _ret = client.write();
                client.close();
                return _ret;
            }
        }

        public boolean providePressureMeasurement(UdpClient client, SensorPressureMeasurement meas) {
            synchronized(client) {
                if(!client.connect()) {
                    return false;
                }
                BaseBuffer buff = client.getBuff();
                buff.putInt(PROTOCOL_TYPE);
                buff.putInt(PROVIDE_PRESSURE_MEASUREMENT);
                buff.putCodable(meas);
                boolean _ret = client.write();
                client.close();
                return _ret;
            }
        }

    }

    public static abstract class LPPeSensorReceiver implements ProtocolHandler {

        public abstract void requestCapabilities(int agpsVersion);

        public abstract void provideCapabilities(int sensorVersion, SensorProvideCapabilities capabilities);

        public abstract void startMeasurement(SensorMeasurementRequest request);

        public abstract void stopMeasurement();

        public abstract void providePressureMeasurement(SensorPressureMeasurement meas);


        public boolean readAndDecode(UdpServerInterface server) {
            if (!server.read()) {
                return false;
            }
            return decode(server);
        }

        @Override
        public int getProtocolType() {
            return PROTOCOL_TYPE;
        }

        @Override
        public boolean decode(UdpServerInterface server) {
            boolean _ret = true;
            BaseBuffer buff = server.getBuff();
            buff.setOffset(4); // skip protocol type
            int _type = buff.getInt();
            switch (_type) {
            case REQUEST_CAPABILITIES: {
                int agpsVersion = (int) buff.getInt();
                requestCapabilities(agpsVersion);
                break;
            }
            case PROVIDE_CAPABILITIES: {
                int sensorVersion = (int) buff.getInt();
                SensorProvideCapabilities capabilities = (SensorProvideCapabilities) buff.getCodable(SensorProvideCapabilities._instance);
                provideCapabilities(sensorVersion, capabilities);
                break;
            }
            case START_MEASUREMENT: {
                SensorMeasurementRequest request = (SensorMeasurementRequest) buff.getCodable(SensorMeasurementRequest._instance);
                startMeasurement(request);
                break;
            }
            case STOP_MEASUREMENT: {
                stopMeasurement();
                break;
            }
            case PROVIDE_PRESSURE_MEASUREMENT: {
                SensorPressureMeasurement meas = (SensorPressureMeasurement) buff.getCodable(SensorPressureMeasurement._instance);
                providePressureMeasurement(meas);
                break;
            }
            default: {
                _ret = false;
                break;
            }
            }
            return _ret;
        }
    }

}
