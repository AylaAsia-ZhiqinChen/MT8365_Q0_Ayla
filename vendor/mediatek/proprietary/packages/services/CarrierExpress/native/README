The bin file 'usp_service' is a native service.
This bin is pre-built by MediaTek internal.


WHAT IT DOES?
=============

usp_service used to write in sys_env via ioctl for boot logo to change as per operator

HOW IT WAS BUILT?
==================

All source/dependency modules of this module are already put in
'vendor/mediatek/libs' folder.


HOW TO USE IT?
==============

The bin file is a system service, and triggered from a system property change that is set from uniservice pack
